image to webm using gemini

This app no longer has any user login/auth flow.

- UI: `/public/index.html`
- API routes: `/api/start`, `/api/poll`, `/api/video`

Note: server-side API routes still require the `GEMINI_API` environment variable to call Gemini.
